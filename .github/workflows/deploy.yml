name: Deploy on push
on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run deploy script locally on the VM
        run: bash '/home/hjg/Next.js/deploy.sh'
