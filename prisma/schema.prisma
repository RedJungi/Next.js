// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 블로그 포스트 모델
model Post {
  id        Int      @id @default(autoincrement()) // ← 기본 키, 자동 증가
  title     String // ← 필수 문자열
  content   String // ← 필수 문자열
  author    String? // ← 선택적 문자열
  published Boolean  @default(false) // ← 기본값 false
  createdAt DateTime @default(now()) // ← 기본값 현재 시간
  updatedAt DateTime @updatedAt // ← 업데이트 시 현재 시간으로 자동 변경

  @@map("posts") // ← 테이블 이름을 posts로 지정
}
